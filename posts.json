---
layout: none
permalink: posts.json
---
{
  "posts": [
    {% for post in site.posts %}
      {
        "slug": "{{ post.title | slugify }}",
        "title": "{{ post.title }}",
        "img": "{{ post.img }}",
        "description": "{{ post.desc }}",
        "date": "{{ post.date | date: '%Y-%m-%d' }}",
        "excerpt": "{{ post.content | strip_html | truncate: 130 }}"
      }
    {% unless forloop.last %},{% endunless %}
    {% endfor %}
  ]
}
